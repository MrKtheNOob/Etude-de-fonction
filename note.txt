I need to make a feedback endpoint to get the 
feedback of the dozen of user ill get
I need to set up a database to store those / database ready
style the calculate button 
// Votre retour est précieux pour nous, et nous nous engageons à améliorer constamment notre produit en fonction de vos besoins et suggestions.
Fix when displaying piecwise function to replace for by <=>
quick ticket:on the feedback section the user should be able to validate the feedback normaly without errors by just pressing the
boolean buttons without writting anything in the text area
tangentes horizontales if none then signal that
when sending the data about the interval style you can use strings like [a;b] and
match over it in the double_function file to apply it to the P function
like :
case [a;b]:
    interval.left_open .. type stuff
but I need first the select tag to indicate that [this] is the interval style for [this] function
(function1 or function2)
Did I note that I need the picewise result to be responsive for mobile ?